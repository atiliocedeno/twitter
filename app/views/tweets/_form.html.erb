<%= form_with(model: @tweet, local: true) do |f| %>
<div class="card">
  <div class="card-body">

<div class= "form-group"> <%= f.label :content %>
<%= f.text_field :content, class: 'form-control' %></div>

<div class= "form-group"> <%= f.submit :Twittear %> </div>
</div>
</div>
<% end %>
